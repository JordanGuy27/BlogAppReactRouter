import React, { Component } from 'react';

class PostsIndex extends Component {
    render() {
        return (
            <div>
                Posts Index
            </div>
        );
    }
}

export default PostsIndex;

//want to change the state of the information coming back as the posts from an array to an object for easier post access